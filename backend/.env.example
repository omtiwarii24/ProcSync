DATABASE_URL=postgresql://postgres:postgres@localhost:15433/govinnovate
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:15433/govinnovate_test
JWT_SECRET=change-me-in-prod
JWT_EXPIRE_MINUTES=480
# AI_PROVIDER: "mock" (default; deterministic, offline, used in tests) or "gemini" (live web-search-grounded discovery).
AI_PROVIDER=mock
# GEMINI_API_KEY: required only when AI_PROVIDER=gemini. Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
